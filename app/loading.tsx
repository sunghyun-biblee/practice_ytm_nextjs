"use client";

import React from "react";
import { BarLoader } from "react-spinners";
import LoadingBar from "@/components/LoadindBar";
const loading = () => {
  return <LoadingBar />;
};

export default loading;
